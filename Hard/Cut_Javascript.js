Список вопросов без повторяющихся дублей

1.12.2024JavaScript Список из 100 вопросов с собеседований Middle
JavaScript разработчика в 2025 году Представляем вам список из 100
вопросов, которые могут встретиться на собеседовании для позиции Middle
JavaScript Developer. Я также добавлю подробное объяснение к каждому
вопросу, чтобы вы могли лучше подготовиться к собеседованию.

А здесь мы собрали огромную подборку вопросов с собеседований по:
Frontend, Javascript, React, Vue.

2.  Что такое замыкание (closure) в JavaScript? Приведи пример
    использования. Ответ: Замыкание — это функция, которая сохраняет
    доступ к переменным своей внешней функции даже после того, как
    внешняя функция завершила выполнение. Пример:javascriptКопировать
    кодfunction outer() { let count = 0; return function inner() {
    count++; console.log(count); }; } const counter = outer();
    counter(); // 1 counter(); // 2 Пояснение: В данном примере
    внутренняя функция inner сохраняет ссылку на переменную count из
    внешней функции outer, несмотря на то, что outer уже завершила
    выполнение.

3.  Чем отличается var, let и const в JavaScript? Ответ: var: Объявляет
    переменную с функциональной областью видимости, которая подвержена
    всплытию (hoisting). let: Объявляет переменную с блочной областью
    видимости, не подвержен всплытию. const: Также объявляет переменную
    с блочной областью видимости, но ее значение нельзя изменить после
    присваивания. Пояснение: Важнейшее отличие между ними — область
    видимости и возможность повторного присваивания.

4.  Что такое «this» в JavaScript? Как он работает? Ответ: this — это
    ключевое слово, которое ссылается на объект, из которого вызывается
    функция. Поведение this зависит от контекста вызова: В методах
    объекта — это сам объект. В обычных функциях — глобальный объект
    (window в браузере или global в Node.js), если не использован
    строгий режим (‘use strict’). В стрелочных функциях this лексически
    привязывается к значению, в котором функция была определена.
    Пояснение: Строгий режим (‘use strict’) может изменить поведение
    this, сделав его undefined в некоторых случаях.

5.  Что такое “hoisting” в JavaScript? Ответ: Hoisting (подъем) — это
    механизм в JavaScript, при котором объявления переменных (с
    использованием var) и функций поднимаются в начало их области
    видимости. Пояснение: Например:javascriptКопировать
    кодconsole.log(a); // undefined var a = 5; Переменная a поднимается,
    но инициализация происходит в месте её объявления.

6.  Что такое асинхронность в JavaScript? Как работают setTimeout и
    setInterval? Ответ: Асинхронность в JavaScript позволяет выполнять
    операции (например, HTTP-запросы или таймеры) без блокировки
    основного потока выполнения. setTimeout запускает функцию через
    заданное время, а setInterval вызывает функцию с заданным
    интервалом. Пояснение: setTimeout: Ожидает заданное количество
    миллисекунд, а затем выполняет переданную функцию. setInterval:
    Выполняет переданную функцию через интервал времени, пока не будет
    отменен.

7.  Что такое промисы (Promises) в JavaScript и как они работают? Ответ:
    Промис — это объект, который представляет собой результат
    асинхронной операции, которая может завершиться успешно или с
    ошибкой. Пояснение:javascriptКопировать кодconst promise = new
    Promise((resolve, reject) => { let success = true; if (success) {
    resolve(‘Success!’); } else { reject(‘Error’); } });
    promise.then(result => console.log(result)).catch(error =>
    console.error(error));

8.  Что такое async/await? Чем это отличается от работы с промисами?
    Ответ: async/await — это синтаксический сахар для работы с
    промисами. async делает функцию асинхронной, а await заставляет её
    ждать выполнения промиса. Пояснение: Это упрощает код и делает его
    более читаемым:javascriptКопировать кодasync function fetchData() {
    let data = await fetch(‘url’); let result = await data.json();
    console.log(result); }

9.  Что такое event loop и как работает очередь событий в JavaScript?
    Ответ: Event Loop — это механизм, который позволяет JavaScript
    выполнять асинхронные операции без блокировки потока. Он состоит из
    двух очередей: очереди задач (Task Queue) и очереди микрозадач
    (Microtask Queue). Пояснение: Микрозадачи (например, обработчики
    промисов) выполняются перед задачами из основной очереди.

10. Объясни разницу между null и undefined. Ответ: null — это значение,
    которое явно указывает на отсутствие значения или объекта. undefined
    — это значение, которое присваивается переменной, которая была
    объявлена, но не инициализирована. Пояснение:javascriptКопировать
    кодlet a; console.log(a); // undefined let b = null; console.log(b);
    // null

11. Что такое прототипы в JavaScript? Ответ: Каждый объект в JavaScript
    имеет скрытое свойство proto, которое указывает на его прототип.
    Прототипы используются для реализации наследования. Пояснение:
    Пример:javascriptКопировать кодfunction Person(name) { this.name =
    name; } Person.prototype.sayHello = function() {
    console.log(Hello, ${this.name}); }; const john = new
    Person(‘John’); john.sayHello(); // “Hello, John”

12. Что такое map, filter, reduce и в чем их отличие? Ответ: map —
    создает новый массив, применяя функцию ко всем элементам исходного
    массива. filter — создает новый массив, включающий только те
    элементы, которые проходят тест. reduce — преобразует массив в одно
    значение (например, сумма элементов). Пояснение:javascriptКопировать
    кодlet arr = [1, 2, 3, 4]; let mapped = arr.map(x => x * 2); // [2,
    4, 6, 8] let filtered = arr.filter(x => x > 2); // [3, 4] let sum =
    arr.reduce((acc, x) => acc + x, 0); // 10

13. Что такое деструктуризация в JavaScript? Ответ: Деструктуризация
    позволяет извлекать значения из массивов или объектов и присваивать
    их переменным. Пояснение:javascriptКопировать кодconst person = {
    name: ‘John’, age: 30 }; const { name, age } = person;
    console.log(name); // John console.log(age); // 30

14. Что такое REST и Spread операторы? Ответ: REST — используется для
    сбора оставшихся элементов в массив или объект (например, в функциях
    или при деструктуризации). Spread — используется для распространения
    элементов массива или объекта. Пояснение:javascriptКопировать
    кодconst arr = [1, 2, 3]; const newArr = […arr, 4]; // [1, 2, 3, 4]
    function sum(…numbers) { return numbers.reduce((acc, num) => acc +
    num, 0); } console.log(sum(1, 2, 3)); // 6

15. Как работает оператор typeof? Ответ: Оператор typeof используется
    для проверки типа значения. Пояснение:javascriptКопировать
    кодconsole.log(typeof 42); // ‘number’ console.log(typeof ‘hello’);
    // ‘string’ console.log(typeof undefined); // ‘undefined’

16. Что такое call, apply и bind? Ответ: Эти методы позволяют изменять
    контекст this в функциях. call и apply вызывают функцию немедленно,
    но с разными способами передачи аргументов. bind возвращает новую
    функцию с привязанным контекстом.

17. Что такое promise chaining? Ответ: Promise chaining — это
    последовательное выполнение нескольких асинхронных операций с
    использованием метода then. Пояснение:javascriptКопировать
    кодfetch(‘url’) .then(response => response.json()) .then(data =>
    console.log(data)) .catch(error => console.error(error));

18. Что такое модульная система в JavaScript? Ответ: Модульная система
    позволяет разделить код на отдельные файлы (модули) для улучшения
    структуры и повторного использования. Пояснение: В ECMAScript 6
    появились нативные модули, которые используют import и
    export.javascriptКопировать код// person.js export const person = {
    name: ‘John’ }; // app.js import { person } from ‘./person’;
    console.log(person.name); // John

19. Что такое Symbol в JavaScript? Ответ: Symbol — это примитивный тип
    данных, который представляет уникальные и неизменяемые
    идентификаторы. Пояснение:javascriptКопировать кодconst sym1 =
    Symbol(‘description’); const sym2 = Symbol(‘description’);
    console.log(sym1 === sym2); // false

20. Что такое мемоизация? Ответ: Мемоизация — это оптимизация функций,
    при которой результаты выполнения функции кэшируются для повторного
    использования с теми же аргументами. Пояснение: Мемоизация помогает
    улучшить производительность при повторном вызове одинаковых функций
    с одинаковыми параметрами.

21. Что такое “event delegation”? Ответ: Делегация событий — это
    техника, при которой событие обрабатывается на родительском
    элементе, а не на каждом дочернем. Это позволяет оптимизировать
    обработку событий. Пояснение:javascriptКопировать
    кодdocument.getElementById(‘parent’).addEventListener(‘click’,
    function(event) { if (event.target &&
    event.target.matches(‘button’)) { console.log(‘Button clicked’); }
    });

22. Как работают дебаунсинг и троттлинг? Ответ: Это техники для
    управления количеством вызовов функции в определенный промежуток
    времени. Дебаунсинг задерживает выполнение функции до тех пор, пока
    не прекратится серия вызовов. Троттлинг ограничивает количество
    вызовов функции в заданный промежуток времени.

23. Что такое прокси (Proxy) в JavaScript? Ответ: Прокси — это объект,
    который оборачивает другой объект и позволяет перехватывать и
    изменять операции с ним (например, геттеры, сеттеры).
    Пояснение:javascriptКопировать кодconst person = { name: ‘John’ };
    const proxy = new Proxy(person, { get(target, prop) { return prop in
    target ? target[prop] : ‘Not found’; } }); console.log(proxy.name);
    // John console.log(proxy.age); // Not found

24. Объясни, как работают стрелочные функции. Ответ: Стрелочные функции
    — это функции с более кратким синтаксисом и лексическим связыванием
    this. Пояснение: Стрелочная функция не имеет своего контекста this,
    и использует значение из родительского
    контекста.javascriptКопировать кодconst obj = { name: ‘Alice’,
    greet: () => { console.log(this.name); // undefined } };
    obj.greet();

25. Какие способы клонирования объектов существуют в JavaScript? Ответ:
    Клонирование объектов можно сделать несколькими способами:
    Использование Object.assign() Использование оператора spread (…)
    Использование JSON.parse() и JSON.stringify() (для глубокого
    клонирования, но с ограничениями).

26. Что такое гибридные приложения? Это приложения, которые сочетают в
    себе элементы как веб-приложений, так и нативных мобильных
    приложений. Они разрабатываются с использованием веб-технологий
    (HTML, CSS, JavaScript) и затем оборачиваются в нативные контейнеры,
    что позволяет запускать их на различных мобильных платформах (iOS,
    Android и др.).

🚩Основные характеристики

🟠Кроссплатформенность

Одним из главных преимуществ гибридных приложений является возможность
работы на различных платформах без необходимости писать отдельный код
для каждой платформы.

🟠Использование веб-технологий

Гибридные приложения разрабатываются с использованием стандартных
веб-технологий (HTML, CSS, JavaScript), что упрощает процесс разработки
для веб-разработчиков.

🟠Нативные контейнеры

Эти приложения оборачиваются в нативные контейнеры (например, с помощью
Apache Cordova или Capacitor), что позволяет им использовать функции и
API нативных платформ.

🚩Примеры технологий и фреймворков

🟠Apache Cordova (ранее PhoneGap)

Фреймворк для создания мобильных приложений с использованием
веб-технологий. Позволяет оборачивать веб-приложения в нативные
контейнеры.

🟠Ionic

Популярный фреймворк для разработки гибридных мобильных приложений.
Основан на Angular и Cordova, но также поддерживает другие фреймворки,
такие как React и Vue.

🟠Capacitor

Современная альтернатива Cordova, разработанная командой Ionic.
Предоставляет более мощные и гибкие возможности для интеграции с
нативными функциями.

🟠React Native

Хотя React Native обычно рассматривается как фреймворк для
кроссплатформенной разработки нативных приложений, его можно
использовать для создания гибридных приложений, интегрируя веб-контент.

🚩Пример создания

1⃣Установка Ionic CLI

npm install -g @ionic/cli

2⃣Создание нового проекта

ionic start myApp tabs –type=angular

3⃣Запуск приложения в браузере

cd myApp

ionic serve

4⃣Сборка и запуск на мобильном устройстве

ionic capacitor add android

ionic capacitor run android

🚩Плюсы

➕Снижение затрат на разработку

Разработка и поддержка единой кодовой базы для нескольких платформ
позволяет сократить затраты на разработку и время выхода на рынок.

➕Кроссплатформенность

Возможность запуска на различных платформах без необходимости
переписывать код для каждой платформы.

➕Использование существующих навыков

Веб-разработчики могут использовать свои знания HTML, CSS и JavaScript
для создания мобильных приложений.

🚩Минусы

➖Производительность

Гибридные приложения могут работать медленнее по сравнению с нативными
приложениями, особенно при выполнении сложных графических операций или
интенсивного использования ресурсов.

➖Ограниченный доступ к нативным функциям

Хотя большинство нативных функций доступны через плагины, некоторые из
них могут быть ограничены или требовать дополнительных усилий для
интеграции.

➖Пользовательский интерфейс

Достижение нативного пользовательского интерфейса может быть сложным, и
гибридные приложения могут не всегда соответствовать ожиданиям
пользователей по внешнему виду и поведению.

Для старшего разработчика JavaScript (Senior JavaScript Developer)
вопросы на собеседованиях становятся более глубокими и требующими опыта
в решении сложных задач, а также владения современными инструментами и
практиками разработки. В этой статье мы продолжим тему и представим ещё
25 вопросов, которые могут быть заданы на собеседовании для этой роли, с
развернутыми ответами и объяснениями.

27. Чем отличается == от === в JavaScript? Ответ:

== — это оператор сравнения с приведением типов (loose equality). Он
пытается преобразовать операнды к одинаковому типу перед сравнением. ===
— это строгий оператор сравнения (strict equality), который сравнивает
значения и типы без преобразования типов. Пример:

javascriptКопировать кодconsole.log(1 == ‘1’); // true console.log(1 ===
‘1’); // false

28. Что такое “async/await” и как их использовать? Ответ: async/await —
    это синтаксический сахар для работы с асинхронным кодом, который
    делает его более читаемым и похожим на синхронный код.

async используется для объявления функции, которая всегда возвращает
промис. await используется внутри асинхронных функций для ожидания
выполнения промиса. Пример:

javascriptКопировать кодasync function fetchData() { const response =
await fetch(‘https://api.example.com’); const data = await
response.json(); return data; }

29. Как работает механизм обработки ошибок с использованием try/catch?
    Ответ: try/catch — это конструкция для обработки ошибок в
    JavaScript. Внутри блока try выполняется код, а если возникает
    ошибка, управление передается в блок catch.

Пример:

javascriptКопировать кодtry { const result = someFunction(); } catch
(error) { console.error(‘Error:’, error); }

30. Что такое “IIFE” (Immediately Invoked Function Expression)? Ответ:
    IIFE — это функция, которая немедленно выполняется после её
    объявления. Она часто используется для создания замкнутого контекста
    и предотвращения загрязнения глобального пространства имен.

Пример:

javascriptКопировать код(function() { console.log(‘This function runs
immediately’); })();

31. Как работает механизм событий в JavaScript (Event Bubbling и
    Capturing)? Ответ:

Event Bubbling (всплытие события) — это процесс, при котором событие
сначала обрабатывается на самом вложенном элементе, а затем по цепочке
всплывает вверх к родительским элементам. Event Capturing (поглощение
события) — это процесс, при котором событие сначала обрабатывается на
самом верхнем элементе, а затем по цепочке передается вниз к дочерним
элементам. Пример:

htmlКопировать код

Click me

javascriptКопировать
кодdocument.getElementById(‘parent’).addEventListener(‘click’,
function() { console.log(‘Parent clicked’); }, true); // capturing

document.getElementById(‘child’).addEventListener(‘click’, function() {
console.log(‘Child clicked’); }); // bubbling

32. Что такое “defer” и “async” в тегах
    ? Ответ: Атрибуты defer и async используются в тегах
    для управления загрузкой и выполнением JavaScript на веб-странице.

defer: Скрипт с атрибутом defer загружается асинхронно, но выполняется
только после того, как весь HTML документ будет полностью загружен
(после события DOMContentLoaded). Скрипты с defer выполняются в порядке
их появления на странице. htmlКопировать код
async: Скрипт с атрибутом async загружается асинхронно и выполняется
сразу после того, как он будет загружен, без ожидания загрузки других
скриптов или HTML. Порядок выполнения может быть произвольным, так как
скрипты могут загружаться параллельно. htmlКопировать код
33. Что такое “this” в стрелочных функциях? Ответ: В стрелочных функциях
    this не привязывается к самому объекту, а наследуется от окружающего
    контекста, в отличие от обычных функций, где this привязывается к
    объекту, который вызывает функцию.

Пример:

javascriptКопировать кодconst obj = { value: 42, regularFunction:
function() { console.log(this.value); // 42, так как this указывает на
obj }, arrowFunction: () => { console.log(this.value); // undefined, так
как this наследуется от окружающего контекста (глобального объекта) } };

obj.regularFunction(); obj.arrowFunction();

34. Что такое Shadow DOM? Ответ: Shadow DOM — это механизм инкапсуляции,
    который позволяет скрыть часть DOM от основного дерева. Это полезно
    для создания компонентов с независимым стилем и поведением,
    предотвращая влияние на другие части страницы.

Пример:

htmlКопировать код

35. Что такое “currying” в JavaScript? Ответ: Currying — это процесс
    преобразования функции, которая принимает несколько аргументов, в
    серию функций, каждая из которых принимает один аргумент. Это
    позволяет создавать более универсальные и частично применяемые
    функции.

Пример:

javascriptКопировать кодconst add = a => b => a + b;

const add5 = add(5); console.log(add5(3)); // 8

36. Что такое “polyfill” и зачем он нужен? Ответ: Polyfill — это код,
    который добавляет поддержку новых возможностей в старые браузеры.
    Это позволяет использовать новые функции JavaScript, такие как
    Promise, fetch, Object.assign, в браузерах, которые их не
    поддерживают.

Пример polyfill для Array.prototype.includes:

javascriptКопировать кодif (!Array.prototype.includes) {
Array.prototype.includes = function(element) { return
this.indexOf(element) !== -1; }; }

37. Что такое “webpack” и как он работает? Ответ: Webpack — это
    модульный бандлер для JavaScript-приложений. Он анализирует
    зависимости между файлами и создаёт оптимизированный бандл для
    загрузки на клиенте. Webpack позволяет использовать различные
    плагины и загрузчики для обработки стилей, изображений, шрифтов и
    других активов.

Основные понятия:

entry: точка входа в приложение. output: куда будет собран конечный
бандл. loaders: обработчики для файлов (например, для компиляции SCSS в
CSS). plugins: дополнения для расширения функционала (например, для
минимизации кода). Пример базовой конфигурации:

javascriptКопировать кодmodule.exports = { entry: ‘./src/index.js’,
output: { filename: ‘bundle.js’, path: path.resolve(__dirname, ‘dist’) }
};

38. Что такое “service workers” в JavaScript? Ответ: Service Workers —
    это скрипты, которые работают в фоновом режиме и могут
    использоваться для реализации функционала, такого как кэширование
    данных для работы в оффлайн-режиме, push-уведомления и фоновые
    синхронизации.

Пример регистрации service worker:

javascriptКопировать кодif (‘serviceWorker’ in navigator) {
navigator.serviceWorker.register(‘/service-worker.js’)
.then(function(registration) { console.log(‘Service Worker
зарегистрирован:’, registration); }) .catch(function(error) {
console.log

20 Вопросов с Ответами для Собеседования Angular Разработчика Angular —
это мощный фреймворк для разработки веб-приложений, и собеседования на
должность Angular-разработчика требуют знаний как основных концепций
фреймворка, так и более глубоких технических аспектов. В этой статье мы
рассмотрим 20 важных вопросов, которые могут быть заданы на
собеседовании для Angular-разработчика, с подробными объяснениями.

39. Что такое Angular и чем он отличается от других фреймворков
    (например, React или Vue)? Ответ: Angular — это полноценный
    фреймворк для разработки веб-приложений от Google. Он использует
    TypeScript, предлагает двустороннюю привязку данных, инжекцию
    зависимостей, маршрутизацию, формы, HTTP-клиент и много других
    встроенных возможностей. В отличие от React и Vue, которые
    фокусируются на представлении (View) и предоставляют минимальную
    функциональность, Angular — это более монолитный фреймворк, который
    включает в себя все необходимые инструменты для создания крупных
    приложений.

40. Что такое компоненты в Angular? Ответ: Компонент в Angular — это
    основной строительный блок приложения. Он состоит из:

Шаблона (HTML), который определяет представление. Класса (TypeScript),
который управляет логикой компонента. Стилей (CSS), которые управляют
внешним видом. Каждый компонент имеет декоратор @Component, который
указывает Angular, что это компонент и какие шаблон, стили и другие
настройки ему присвоены.

Пример:

typescriptКопировать код@Component({ selector: ‘app-my-component’,
templateUrl: ‘./my-component.component.html’, styleUrls:
[‘./my-component.component.css’] }) export class MyComponent { title:
string = ‘Hello, Angular!’; }

41. Что такое директивы в Angular и чем они отличаются от компонентов?
    Ответ: Директивы — это классы, которые позволяют манипулировать
    элементами DOM. В отличие от компонентов, которые могут содержать
    шаблон и логику, директивы не имеют собственного представления. Они
    используются для изменения поведения DOM-элементов.

Существует три типа директив:

Структурные директивы (ngIf, ngFor) — изменяют структуру DOM. Атрибутные
директивы (например, ngClass, ngStyle) — изменяют внешний вид или
поведение элементов. Компоненты — можно рассматривать как директивы с
шаблоном. Пример атрибутной директивы:

htmlКопировать код<div [ngClass]=“{ ‘highlight’: isHighlighted }”>Hello,
Angular!
42. Что такое двухсторонняя привязка данных (two-way data binding) в
    Angular? Ответ: Двухсторонняя привязка данных позволяет
    синхронизировать данные между компонентом и его представлением.
    Когда данные изменяются в компоненте, изменения автоматически
    отображаются в шаблоне, и наоборот — при изменении данных в шаблоне
    они автоматически обновляются в компоненте.

Пример:

htmlКопировать код<input [(ngModel)]=“username”>
Your username is: {{ username }}
Здесь изменения в поле ввода автоматически обновляют значение username в
компоненте и наоборот.

43. Что такое сервисы и инъекция зависимостей в Angular? Ответ: Сервис —
    это класс, предназначенный для выполнения логики, которая может быть
    повторно использована в разных компонентах, например, обработка
    данных, запросы к API и т.д. В Angular сервисы обычно инжектируются
    в компоненты или другие сервисы с помощью системы инъекции
    зависимостей.

Пример создания сервиса:

typescriptКопировать код@Injectable({ providedIn: ‘root’ }) export class
MyService { getData() { return ‘Hello, Service!’; } } Использование в
компоненте:

typescriptКопировать кодconstructor(private myService: MyService) {}

ngOnInit() { console.log(this.myService.getData()); }

44. Что такое маршрутизация в Angular? Ответ: Маршрутизация в Angular —
    это механизм, который позволяет навигировать между различными
    представлениями (страницами) приложения. В Angular маршруты
    определяются в файле маршрутизации и связываются с компонентами.

Пример маршрутизации:

typescriptКопировать кодconst routes: Routes = [ { path: ‘home’,
component: HomeComponent }, { path: ‘about’, component: AboutComponent
}];

@NgModule({ imports: [RouterModule.forRoot(routes)], exports:
[RouterModule] }) export class AppRoutingModule {} В шаблоне:

htmlКопировать кодHome

45. Что такое RxJS и как он используется в Angular? Ответ: RxJS
    (Reactive Extensions for JavaScript) — это библиотека для работы с
    асинхронными событиями и потоками данных, которая активно
    используется в Angular для работы с асинхронными операциями
    (например, HTTP-запросами, событиями пользователя). Основной
    абстракцией в RxJS является Observable, который представляет собой
    поток значений, с которым можно работать с помощью операторов, таких
    как map, filter, mergeMap, и т.д.

Пример использования RxJS:

typescriptКопировать кодimport { Observable } from ‘rxjs’;

const observable = new Observable(subscriber => {
subscriber.next(‘Hello’); subscriber.next(‘World’);
subscriber.complete(); });

observable.subscribe({ next(x) { console.log(x); }, complete() {
console.log(‘Completed!’); } });

46. Что такое шаблонные переменные в Angular? Ответ: Шаблонные
    переменные в Angular используются для получения доступа к элементам
    DOM или компонентам в шаблоне. Они могут быть использованы для
    передачи данных в другие компоненты или для манипуляций с DOM.

Пример:

htmlКопировать код<input #inputElement> <button
(click)=“logValue(inputElement.value)”>Log Value В этом примере
#inputElement является шаблонной переменной, которая ссылается на
элемент .

47. Что такое жизненный цикл компонента в Angular? Ответ: Angular
    предоставляет несколько хуков жизненного цикла компонента, которые
    позволяют разработчикам выполнять различные действия в разные
    моменты времени в процессе создания, изменения и уничтожения
    компонента.

Основные хуки:

ngOnInit() — вызывается после инициализации компонента. ngOnChanges() —
вызывается при изменении входных данных компонента. ngDoCheck() —
вызывается при каждом цикле проверки изменений. ngOnDestroy() —
вызывается перед уничтожением компонента. Пример:

typescriptКопировать кодngOnInit() { console.log(‘Component
initialized’); }

48. Что такое Angular CLI и как его использовать? Ответ: Angular CLI
    (Command Line Interface) — это инструмент командной строки, который
    помогает создавать, разрабатывать и обслуживать Angular-приложения.
    Он предоставляет команды для генерации компонентов, сервисов,
    модулей и других элементов, а также для сборки и тестирования
    приложения.

Пример использования:

Создание нового компонента: ng generate component my-component Запуск
приложения: ng serve

49. Что такое анимации в Angular? Ответ: Анимации в Angular позволяют
    создавать визуальные эффекты в приложении. Angular использует модуль
    @angular/animations для создания анимаций, таких как плавные
    переходы между состояниями компонентов.

Пример анимации:

typescriptКопировать кодimport { trigger, state, style, animate,
transition } from ‘@angular/animations’;

@Component({ selector: ‘app-box’, templateUrl: ‘./box.component.html’,
styleUrls: [‘./box.component.css’], animations: [ trigger(‘fadeInOut’, [
state(‘in’, style({ opacity: 1 })), state(‘out’, style({ opacity: 0 })),
transition(‘in <=> out’, animate(1000)) ]) ] }) export class
BoxComponent { state = ‘in’; } В шаблоне:

htmlКопировать код<div [@fadeInOut]=“state”>Content
50. Что такое Angular Forms и чем отличаются template-driven формы от
    reactive forms? Ответ: Angular поддерживает два типа форм:

Template-driven forms: Управляются через шаблон, с использованием
директив ngModel и других атрибутов. Просты в использовании для
небольших форм. Reactive forms: Управляются программно через реактивную
модель, где состояние и валидация формы контролируются в компоненте.
Подходят для сложных форм с динамическими полями. Пример reactive формы:

typescriptКопировать кодimport { FormGroup, FormControl } from
‘@angular/forms’;

this.form = new FormGroup({ name: new FormControl(’’) });

51. Что такое ngIf и ngFor в Angular? Ответ:

ngIf — структурная директива, которая добавляет или удаляет элемент DOM
в зависимости от условия. htmlКопировать код<div *ngIf=“isVisible”>This
is visible
ngFor — используется для итерации по коллекции и создания элементов для
каждого элемента в массиве. htmlКопировать код<div *ngFor=“let item of
items”>{{ item }}
52. Что такое интерцепторы HTTP в Angular? Ответ: Интерцепторы в Angular
    позволяют перехватывать HTTP-запросы и ответы, модифицировать их или
    обрабатывать ошибки. Это полезно для добавления заголовков,
    обработки ошибок или выполнения логирования.

Пример создания интерцептора:

typescriptКопировать код@Injectable() export class AuthInterceptor
implements HttpInterceptor { intercept(req: HttpRequest, next:
HttpHandler) { const clonedReq = req.clone({ setHeaders: {
Authorization: ‘Bearer’ + this.authService.getToken() } }); return
next.handle(clonedReq); } }

53. Что такое lazy loading в Angular? Ответ: Lazy loading — это техника
    загрузки модулей только тогда, когда они действительно необходимы
    (например, при навигации на определённую страницу). Это улучшает
    производительность, так как не нужно загружать все модули сразу при
    старте приложения.

Пример настройки lazy loading:

typescriptКопировать кодconst routes: Routes = [ { path: ‘feature’,
loadChildren: () => import(‘./feature/feature.module’).then(m =>
m.FeatureModule) }];

54. Что такое ngOnChanges() и когда оно вызывается? Ответ: ngOnChanges()
    — это хук жизненного цикла, который вызывается, когда входные данные
    компонента изменяются. Этот метод получает объект с текущими и
    предыдущими значениями входных данных.

Пример:

typescriptКопировать кодngOnChanges(changes: SimpleChanges) {
console.log(changes); }

55. Как управлять состоянием в Angular? Ответ: В Angular можно управлять
    состоянием несколькими способами:

Через сервисы, которые хранят состояние и передают его компонентам. С
помощью RxJS (например, через BehaviorSubject). Использование NgRx —
библиотеки для управления состоянием с использованием Redux-подхода.

56. Что такое providedIn: ‘root’ в Angular? Ответ: providedIn: ‘root’ в
    декораторе @Injectable() означает, что сервис будет предоставлен на
    уровне корневого инжектора и будет доступен во всём приложении.

Пример:

typescriptКопировать код@Injectable({ providedIn: ‘root’ }) export class
MyService { // Логика сервиса }

57. Что такое Change Detection в Angular? Ответ: Change detection
    (обнаружение изменений) в Angular — это процесс проверки изменений в
    данных и обновления DOM для отображения этих изменений. Angular
    использует различные стратегии обнаружения изменений, такие как
    Default и OnPush.

Пример:

typescriptКопировать код@Component({ changeDetection:
ChangeDetectionStrategy.OnPush }) export class MyComponent {}

58. Как работают модули в Angular? Ответ: Модули в Angular — это
    контейнеры для компонентов, сервисов и других объектов, которые
    нужны для работы приложения. Каждый Angular-приложение начинается с
    главного модуля (AppModule), который объединяет все части приложения
    и экспортирует необходимые элементы для других частей.

Пример модуля:

typescriptКопировать код@NgModule({ declarations: [AppComponent],
imports: [BrowserModule], providers: [], bootstrap: [AppComponent] })
export class AppModule {} Эти вопросы и ответы помогут вам подготовиться
к собеседованию на должность Angular-разработчика, охватывая как
основные концепции, так и более сложные аспекты фреймворка.

25 Вопросов с Ответами на Собеседовании для React Разработчика На
собеседованиях для позиции React-разработчика часто задаются вопросы,
которые проверяют как теоретические знания, так и практические навыки. В
этой статье мы рассмотрим 25 популярных вопросов, которые могут быть
заданы на собеседовании, с подробными ответами и объяснениями.

59. Что такое React? Ответ: React — это библиотека JavaScript для
    создания пользовательских интерфейсов. Она разработана Facebook и
    позволяет строить компоненты, которые могут обновлять и рендерить
    интерфейс в ответ на изменения состояния приложения. React
    использует виртуальный DOM для повышения производительности.

60. Что такое JSX? Ответ: JSX (JavaScript XML) — это синтаксический
    сахар для JavaScript, который позволяет писать HTML-подобный код
    внутри JavaScript. JSX превращается в обычный JavaScript с помощью
    компилятора, такого как Babel. Пример:

jsxКопировать кодconst element =
Hello, world!
; JSX упрощает создание и чтение UI компонентов.

61. Что такое Virtual DOM и как он работает? Ответ: Virtual DOM — это
    абстракция реального DOM, которая используется React для оптимизации
    производительности. Когда компонент обновляется, React сначала
    изменяет виртуальный DOM, затем сравнивает его с предыдущей версией
    и вносит минимальные изменения в реальный DOM.

62. Что такое компонент в React? Ответ: Компонент в React — это
    функциональная или классовая сущность, которая принимает входные
    данные (props) и возвращает UI, описанный с использованием JSX.
    Компоненты могут быть повторно используемыми и могут содержать
    логику состояния.

63. Что такое props и state в React? Ответ:

Props (свойства) — это данные, которые передаются в компонент родителем.
Props являются неизменяемыми для компонента. State (состояние) — это
данные, которые управляются внутри компонента и могут изменяться.
Изменение состояния вызывает повторный рендер компонента.

64. Как работает метод setState? Ответ: Метод setState используется для
    обновления состояния компонента в React. Когда состояние изменяется,
    React перерисовывает компонент и его дочерние элементы. Важно, что
    setState работает асинхронно, и обновления могут быть объединены.

jsКопировать кодthis.setState({ counter: this.state.counter + 1 });

65. Что такое жизненный цикл компонента? Ответ: Жизненный цикл
    компонента — это набор методов, которые вызываются на разных стадиях
    существования компонента. Важно знать методы:

componentDidMount — вызывается после первого рендера компонента.
componentDidUpdate — вызывается после обновления компонента.
componentWillUnmount — вызывается перед удалением компонента. С
появлением React Hooks жизненный цикл можно управлять и с помощью хуков
(например, useEffect).

66. Что такое React Hooks? Ответ: React Hooks — это новые возможности,
    введенные в React 16.8, которые позволяют использовать состояние и
    другие возможности React в функциональных компонентах, без
    необходимости писать классовые компоненты. Примеры хуков:

useState — для управления состоянием. useEffect — для работы с побочными
эффектами. useContext — для работы с контекстом.

67. Как работает хук useState? Ответ: useState — это хук, который
    позволяет добавлять состояние в функциональные компоненты. Он
    возвращает массив из двух элементов: текущего состояния и функции
    для его обновления.

jsКопировать кодconst [count, setCount] = useState(0);

68. Что такое хук useEffect? Ответ: useEffect — это хук, который
    позволяет выполнять побочные эффекты в функциональных компонентах
    (например, запросы к API, подписки, манипуляции с DOM). Он
    выполняется после рендера компонента.

jsКопировать кодuseEffect(() => { // код для побочного эффекта },
[dependencies]); // зависимости, по изменению которых будет выполняться
эффект

69. Как работает хук useContext? Ответ: useContext позволяет
    функциональным компонентам использовать значения из контекста. Это
    полезно для передачи данных между компонентами, не прокидывая props
    через всю иерархию.

jsКопировать кодconst value = useContext(MyContext);

70. Что такое React Context? Ответ: React Context — это механизм,
    позволяющий передавать данные через дерево компонентов без
    необходимости вручную передавать props на каждом уровне. Context
    полезен для глобальных данных, таких как тема, авторизация и
    настройки.

71. Что такое Higher-Order Component (HOC)? Ответ: HOC — это функция,
    которая принимает компонент и возвращает новый компонент с
    добавленной функциональностью. HOC часто используется для повторного
    использования логики между компонентами.

jsКопировать кодconst withAuth = (Component) => { return function
AuthHOC(props) { if (!props.isAuthenticated) { return ; } return
<Component {…props} />; }; };

72. Как обрабатываются события в React? Ответ: События в React
    обрабатываются через синтетические события, которые абстрагируют
    реальные события браузера. Например, для обработки клика на кнопку:

jsxКопировать кодfunction handleClick() { console.log(‘Clicked’); }

Click me
73. Что такое key в списках React? Ответ: key — это уникальный
    идентификатор для каждого элемента в списке, который помогает React
    эффективно отслеживать изменения в DOM. key должен быть уникальным
    для каждого элемента в пределах списка.

jsxКопировать кодconst items = [1, 2, 3]; return items.map(item =>
{item}
);

74. Как в React управлять формами? Ответ: Формы в React управляются с
    помощью состояния компонента. Для обработки ввода данных
    используется двухсторонняя привязка. Важный момент — в React
    элементы формы (например, ) контролируются через состояние.

jsxКопировать кодconst [value, setValue] = useState(’’);

const handleChange = (event) => { setValue(event.target.value); };

return ;

75. Что такое мемоизация в React? Ответ: Мемоизация — это процесс
    кэширования результатов вычислений для того, чтобы не выполнять их
    повторно с теми же входными данными. В React мемоизация часто
    используется с помощью React.memo для компонентов и useMemo для
    вычислений.

jsКопировать кодconst MemoizedComponent = React.memo(MyComponent);

76. Что такое динамический импорт в React? Ответ: Динамический импорт
    позволяет загружать модули асинхронно. В React это используется для
    ленивой загрузки компонентов, что улучшает производительность
    приложения. Пример:

jsКопировать кодconst LazyComponent = React.lazy(() =>
import(‘./LazyComponent’));

77. Что такое Redux? Ответ: Redux — это библиотека для управления
    состоянием в JavaScript-приложениях. Он использует принцип единого
    хранилища (store), которое содержит все состояние приложения, и
    позволяет изменять его через действия (actions) и редьюсеры
    (reducers).

78. Что такое “middleware” в Redux? Ответ: Middleware в Redux — это
    промежуточные слои, которые могут перехватывать и обрабатывать
    действия перед тем, как они будут отправлены в редьюсер. Примером
    является redux-thunk, который позволяет использовать асинхронные
    операции.

79. Что такое “pure” компоненты? Ответ: Pure компоненты в React — это
    компоненты, которые не изменяют свои props и state, и всегда
    возвращают одинаковый результат при одинаковых входных данных. Они
    могут быть реализованы с помощью React.PureComponent или
    функциональных компонентов с использованием React.memo.

80. Как работает Suspense в React? Ответ: Suspense — это компонент,
    который позволяет отложить рендеринг части интерфейса до тех пор,
    пока не будут загружены необходимые ресурсы (например, компоненты
    или данные). Это часто используется в сочетании с React.lazy.

81. Как оптимизировать производительность React-приложений? Ответ:
    Некоторые способы оптимизации:

Использование React.memo и useMemo для предотвращения лишних рендеров.
Ленивая загрузка компонентов с помощью React.lazy и Suspense. Разбиение
компонента на более мелкие части. Использование shouldComponentUpdate
или PureComponent.

82. Что такое серверный рендеринг (SSR) в React? Ответ: Серверный
    рендеринг (SSR) — это процесс рендеринга React-компонентов на
    сервере перед отправкой HTML-кода на клиент. Это улучшает SEO и
    ускоряет первоначальную загрузку приложения.

